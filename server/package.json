{
  "name": "silent-wake-relay",
  "private": true,
  "version": "0.1.0",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "migrate": "node scripts/migrate.js",
    "cleanup:relay": "node scripts/cleanup_relay_expiry.js",
    "cleanup:wake": "node scripts/cleanup_wake_tokens.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "sqlite3": "^5.1.7"
  }
}
